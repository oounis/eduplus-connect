import { Loader } from "@/components/kogia";

/** Route transitions inside the app: the whale's wake, with words. */
export default function Loading() {
  return <Loader kind="sillage" label="Loading…" />;
}
